name: Functions
version: 1.0.1
summary: Azure Functions with Storage environment
description: Deploys functions, storage, application insights
runner: ARM
templatePath: funcapp.json
parameters:
- id: "prefix"
  name: "prefix"
  description: "prefix name for unique id"
  default: ""
  type: "string"
  required: true
- id: "region"
  name: "region"
  description: "Azure region for deployed resources"
  default: ""
  type: "string"
  required: true
