name: Container_infra
version: 1.0.0
summary: Azure Container services infra resources
description: Deploys container registry, log analytics, application insights
runner: ARM
templatePath: infra.json
parameters:
- id: "prefix"
  name: "prefix"
  description: "prefix name for unique id"
  default: ""
  type: "string"
  required: true
- id: "region"
  name: "region"
  description: "Azure region for deployed resources"
  default: ""
  type: "string"
  required: true
